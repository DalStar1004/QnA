# QnA 프로젝트 안내

단어 연결 게임(`word_connection_game.html`)을 중심으로 한 프로젝트입니다.

## 작업 기록은 매번 남긴다

**작업을 하나 마칠 때마다** `docs/작업기록/작업기록.md` 맨 아래에 항목을 추가한다.
사용자가 따로 요청하지 않아도 기록을 남기며, 형식은 다음과 같다.

```markdown
## 00N · YYYY-MM-DD · 한 줄 요약

**요청한 내용**

> 사용자가 입력한 프롬프트 (원문 그대로)

**AI가 한 일**

1. 무엇을 어떻게 바꿨는지 (파일/함수 이름 포함)
2. 왜 그렇게 했는지

**검증** — 어떻게 확인했는지
```

- 파일 맨 위의 요약 표에도 같은 번호로 한 줄을 추가한다.
- 프롬프트 원문은 `docs/작업기록/프롬프트-원문.md`에 훅이 자동으로 쌓으므로 손대지 않는다.
- 자세한 사용법은 `docs/작업기록/README.md` 참고.

## 이 폴더만으로 돌아가야 한다

게임과 서버는 **QnA 폴더 안의 것만으로** 실행되어야 한다. 새 의존성을 넣을 때는
인터넷에서 받아 쓰지 말고 폴더 안에 넣는다.

- 글꼴은 `assets/fonts/` · `server/public/assets/fonts/` 에 있다. CDN(`fonts.googleapis.com`)을 쓰지 않는다.
- 서버 실행에 쓰는 Node.js는 `tools/node/`, 라이브러리는 `server/node_modules/` 에 있다.
- 예외는 AI 스무고개의 문장 힌트를 만드는 Groq API(무료 티어, 기본 모델 `qwen/qwen3.8-27b`)
  하나뿐이며, 없어도 게임은 진행되어야 한다.
- **API 키는 게임 화면의 [연결하기] 에서 넣는다.** 서버는 키 파일도 환경변수도 읽지 않고,
  받은 키를 메모리에만 들고 있다(파일로 쓰거나 로그에 찍지 않는다).
  [연결 끊기] 를 누르거나 서버가 다시 켜지면 사라진다. **키를 소스나 저장소에 넣지 않는다.**
- 키가 새거나 남이 대신 쓰지 못하게 막아 두었다 — 커밋 훅(`.githooks/pre-commit`,
  `git config core.hooksPath .githooks` 로 켠다), 한 사람 30회/10분·하루 500회 제한,
  다른 사이트에서 오는 요청 거절. 이 장치들을 약하게 고치지 않는다.
  혼자 하기도 브라우저가 Groq 를 직접 부르지 않고 서버의 `/api/ai/*` 를 거치므로,
  키가 브라우저로 내려가지 않는다.
- **검사용 스크립트가 `groq-key.txt` 를 쓰거나 지우지 않는다.** 사용자의 진짜 키가 들어 있다.

## 게임 파일 작업 시 참고

- `word_connection_game.html`은 HTML/CSS/JS가 한 파일에 들어 있는 단일 파일 게임이다.
- 큰 수정 전에는 원본을 백업하고, 수정 후에는 브라우저에서 실제로 동작을 확인한다.
- 사용자 대상 문구는 모두 한국어로 작성한다.
